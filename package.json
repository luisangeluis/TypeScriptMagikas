{
  "name": "node-starter",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc hola.ts"
  },
  "devDependencies": {
    "typescript": "^4.9.5"
  }
}
